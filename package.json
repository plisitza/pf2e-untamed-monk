{
  "name": "pf2e-untamed-monk",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "release": "npm run build && rm -f pf2e-untamed-monk.zip && zip -r pf2e-untamed-monk.zip module.json README.md LICENSE packs scripts"
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^3.0.0"
  }
}
